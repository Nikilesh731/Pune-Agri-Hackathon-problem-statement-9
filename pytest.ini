[pytest]
# Avoid collecting UTF-16/other-encoded .txt files as doctests
# Collect doctests only from Markdown files
; Some pytest versions don't support doctest_glob in ini, so explicitly ignore
; problematic text files produced by tools.
collect_ignore = ai-services/test_output.txt
