# AI Services Requirements
# Purpose: Python dependencies for FastAPI AI microservices

fastapi==0.110.0
uvicorn[standard]==0.27.1

pydantic==2.6.4
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

httpx==0.27.0
aiofiles==23.2.1

Pillow>=10.4.0
pytesseract==0.3.10
opencv-python-headless==4.9.0.80  # Headless version for container environments

numpy==1.26.4
pandas==2.2.1
scikit-learn==1.4.1.post1

nltk==3.8.1
transformers==4.38.2

# PDF and OCR processing    
pdfplumber==0.10.3
PyMuPDF==1.23.26

# Document processing for Word documents
python-docx==1.1.0

# PaddleOCR for image OCR (CPU inference) - CONTAINER COMPATIBLE VERSIONS
# Use headless versions and specific versions to avoid GUI library dependencies
paddlepaddle==2.6.1
paddleocr==2.7.3
# Alternative: If PaddleOCR continues to fail, uncomment these lines and comment out paddleocr above
# easyocr==1.7.1  # Fallback OCR engine that works better in containers
# pytesseract==0.3.10  # Keep as legacy fallback